export interface ContentCategories {
    contentCategoryId: number;
    name: string;
    slug?: string;
    description: string;
}

export interface Category {
    categoryId: number;
    slug: string;
    name: string;
}